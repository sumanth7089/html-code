git clone //url

git init <repo-name>

create a file & do work 

git add to stagging 

and  then check status 

git commit -m " "


git logs to get logs

git logs --oneline

git show comit id 

git branch to know head is on which file 

git branch <branch-name>

git checkout <branch-name>

 Do work in branch & Merge Changes/Branch
				
				> Merging - Update the changes from one branch to another branch

					> git merge <branch_name>
 this is too cool 	
 to update all things use git push --all 
 
git reflog -> gives all commit's from day1

merge-conflict: git reflog

to push without user id and password use ssh auth 

password base auth is required for push, when we not  use ssh 
